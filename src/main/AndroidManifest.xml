<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dariapro.traincounting">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.dariapro.traincounting.activity.LevelListActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activity.LevelListActivity">
        </activity>
        <activity android:name="com.dariapro.traincounting.activity.ExamplesListActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activity.ExamplesListActivity">
        </activity>
        <activity android:name="com.dariapro.traincounting.activity.ExamplesPagerActivity"
            android:label="@string/app_name"
            android:parentActivityName=".activity.ExamplesPagerActivity">
        </activity>
    </application>

</manifest>